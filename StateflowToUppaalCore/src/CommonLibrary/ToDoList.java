package CommonLibrary;

public class ToDoList {
	//for every transition before state,set state now pos the global variable.
	//for every event stack transition,add a transition which connect to dest without count to 0.pre transition change to while loop.
}
